name: Pull Confirmed Kalshi Series
on:
  workflow_dispatch:
    inputs:
      date:
        required: false
        default: ''
jobs:
  pull:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Set date
        run: |
          DATE="${{ github.event.inputs.date }}"
          [ -z "$DATE" ] && DATE=$(TZ='America/New_York' date +%Y-%m-%d)
          echo "DATE=$DATE" >> $GITHUB_ENV
      - name: Pull confirmed series
        run: python3 scripts/pull_confirmed.py ${{ env.DATE }}
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/kalshi_confirmed_series.json
          git diff --cached --quiet && echo "No changes" || \
            git commit -m "kalshi confirmed series $(TZ='America/New_York' date +'%Y-%m-%d %H:%M ET')" && git push
